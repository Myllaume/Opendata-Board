// $('.total-col').each(function( i ) {
//     var totalCell = $(this);
//     totalCell.parent().each(function( i ) {
//         var total = 0
//         var cellsToSum = $( this ).children().each(function( i ) {
//             if ($(this).hasClass('sum-col')) {
//                 total += $(this)
//             }
//         })
//     })
// })